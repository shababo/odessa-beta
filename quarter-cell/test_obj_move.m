deltaZ = 0;
deltaY = 420;
deltaX = 0;

num_moves = 20;

for i = 1:num_moves
    isSnapImage = 1;
    pause(1)
    deltaZ = -1*deltaZ;
    deltaX = -1*deltaX;
    deltaY = -1*deltaY;
    isMoveStage = 1;
    pause(1)
%     isSnapImage = 1;
%     pause(1)
    deltaZ = -1*deltaZ;
    deltaX = -1*deltaX;
    deltaY = -1*deltaY;
    isMoveStage = 1;
    pause(1)
end